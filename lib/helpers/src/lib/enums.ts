export enum FeatureType {
  Models = 'Models',
  Tools = 'Tools',
}

export enum MultisignAction {
  AddCollector = 'AddCollector',
  RemoveCollector = 'RemoveCollector',
  RemoveResourceAccount = 'RemoveResourceAccount',
}
